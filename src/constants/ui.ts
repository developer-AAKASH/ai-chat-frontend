/** Stagger (seconds) between each dot in a three-dot "typing"/"thinking" animation. */
export const TYPING_DOT_DELAY_STEP_S = 0.2;

/** Max height (px) the chat input textarea grows to before it starts scrolling. */
export const CHAT_INPUT_MAX_HEIGHT_PX = 160;